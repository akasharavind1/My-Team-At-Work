import { FiltersSamplePipe } from './filters-sample.pipe';

describe('FiltersSamplePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersSamplePipe();
    expect(pipe).toBeTruthy();
  });
});
